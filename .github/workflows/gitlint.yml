# SPDX-FileCopyrightText: Common Ground Electronics <https://cgnd.dev>
#
# SPDX-License-Identifier: CC0-1.0

name: gitlint

on: # yamllint disable-line rule:truthy
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  gitlint:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        # yamllint disable-line rule:line-length
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0

      - name: Install uv
        # yamllint disable-line rule:line-length
        uses: astral-sh/setup-uv@1e862dfacbd1d6d858c55d9b792c756523627244 # v7.1.4
        with:
          enable-cache: true
          cache-dependency-glob: .pre-commit-config.yaml

      - name: Run gitlint
        run: |
          if [ -n "$GITHUB_BASE_REF" ]; then
            RANGE="origin/$GITHUB_BASE_REF..HEAD"
            uv run --no-sync --with gitlint gitlint --commits "$RANGE"
          elif [ -n "$GITHUB_REF_NAME" ]; then
            RANGE="origin/$GITHUB_REF_NAME..HEAD"
            uv run --no-sync --with gitlint gitlint --commits "$RANGE"
          else
            # Manual dispatch: run gitlint only on HEAD
            uv run --no-sync --with gitlint gitlint --commit HEAD
          fi
